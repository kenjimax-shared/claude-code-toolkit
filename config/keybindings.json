{
  "$schema": "https://www.schemastore.org/claude-code-keybindings.json",
  "$docs": "https://code.claude.com/docs/en/keybindings",
  "bindings": [
    {
      "context": "Chat",
      "bindings": {
        "shift+tab": null,
        "alt+shift+tab": "chat:cycleMode"
      }
    },
    {
      "context": "Confirmation",
      "bindings": {
        "shift+tab": null,
        "alt+shift+tab": "confirm:cycleMode"
      }
    }
  ]
}
