#!/usr/bin/env bash
# Wrapper for `claude` command.
# By default, uses the shared Chrome debug profile (port 9222).
# For isolated Chrome sessions, use `claude-isolated` instead.
exec /home/kenji/.local/bin/claude "$@"
